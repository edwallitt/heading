{
  "name": "heading-sim",
  "version": "0.0.0",
  "private": true,
  "description": "Heading — an AI flight-picker for MSFS 2024 (Phase 0 scaffold)",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"pnpm --filter @heading/server dev\" \"pnpm --filter @heading/web dev\"",
    "build": "pnpm --filter @heading/shared build && pnpm --filter @heading/server build && pnpm --filter @heading/web build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  }
}

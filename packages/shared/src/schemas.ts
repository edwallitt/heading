/**
 * Shared Zod schemas live here.
 *
 * Phase 0 has none yet — flight/dispatch/export schemas arrive in later phases.
 * `zod` is wired in so these can be added without touching package wiring.
 */
export {};
